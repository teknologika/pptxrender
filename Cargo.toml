[package]
name = "pptxrender"
version = "0.1.0"
edition = "2021"
description = "CLI tool to render PPTX files into slide images using PowerPoint + macOS PDFKit"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
